import { Button } from "../../Button";
import { Nav } from "./styles";

export const Menu = () => {
    return (
        <Nav>
            <Button>Todes</Button>
            <Button>Alien</Button>
            <Button>Humanes</Button>
        </Nav>
    )
}